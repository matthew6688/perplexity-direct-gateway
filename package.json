{
  "name": "perplexity-direct-gateway",
  "version": "1.0.0",
  "description": "Direct HTTP gateway for Perplexity AI — Aurora-equivalent using cookie auth instead of browser automation",
  "type": "module",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test:sse": "node src/perplexity.sse.test.mjs"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  }
}
